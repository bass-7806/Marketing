{
  "name": "bwell-website",
  "version": "1.0.0",
  "private": true,
  "description": "bwell.co.th — static site generated from src/",
  "scripts": {
    "build": "node build.js",
    "serve": "node serve.js",
    "start": "node build.js && node serve.js"
  },
  "engines": { "node": ">=18" }
}
